import React from 'react';
import './Loader.css';

const Loader = () => {
    return (
        <div className='loader'>
            <div>Playlist loading ...</div>
        </div>
    );
};

export default Loader;